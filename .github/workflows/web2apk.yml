name: 🌐 Web to APK

on:
  workflow_dispatch:
    inputs:
      web_url:
        description: '🌍 URL Website'
        required: true
        type: string
      app_name:
        description: '📱 Nama Aplikasi'
        required: true
        type: string
      icon_url:
        description: '🖼️ URL Icon (PNG/JPG, 512x512 min)'
        required: true
        type: string

env:
  JAVA_VERSION: '17'
  ANDROID_SDK_ROOT: /usr/local/android-sdk
  GRADLE_OPTS: '-Xmx4g -XX:MaxMetaspaceSize=512m'

jobs:
  build:
    name: 🚀 Build WebView APK
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      actions: read

    steps:
      # ─── 1. CHECKOUT ──────────────────────────────────────────────
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      # ─── 2. SETUP JAVA ─────────────────────────────────────────────
      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: ${{ env.JAVA_VERSION }}

      # ─── 3. SETUP ANDROID SDK ──────────────────────────────────────
      - name: 🤖 Setup Android SDK
        run: |
          sudo mkdir -p $ANDROID_SDK_ROOT
          sudo chown -R $USER:$USER $ANDROID_SDK_ROOT
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
          unzip -q cmdline-tools.zip -d $ANDROID_SDK_ROOT
          mv $ANDROID_SDK_ROOT/cmdline-tools $ANDROID_SDK_ROOT/latest
          mkdir -p $ANDROID_SDK_ROOT/cmdline-tools
          mv $ANDROID_SDK_ROOT/latest $ANDROID_SDK_ROOT/cmdline-tools/
          export PATH=$PATH:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
        env:
          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64

      # ─── 4. VALIDASI URL ──────────────────────────────────────────
      - name: 🔍 Validasi URL
        run: |
          URL="${{ github.event.inputs.web_url }}"
          if [[ ! "$URL" =~ ^https?:// ]]; then
            echo "❌ URL tidak valid: $URL"
            exit 1
          fi
          echo "✅ URL valid: $URL"
          curl -I -s "$URL" | head -5 || true

      # ─── 5. DOWNLOAD ICON ─────────────────────────────────────────
      - name: 🖼️ Download icon
        run: |
          ICON_URL="${{ github.event.inputs.icon_url }}"
          echo "📥 Download icon dari: $ICON_URL"
          curl -L -o icon_raw.png "$ICON_URL"
          if [ ! -f icon_raw.png ]; then
            echo "❌ Gagal download icon"
            exit 1
          fi
          # Install imagemagick untuk resize
          sudo apt-get update -qq
          sudo apt-get install -yqq imagemagick
          # Buat berbagai ukuran untuk icon
          mkdir -p app/src/main/res
          for size in 48 72 96 144 192 512; do
            mkdir -p app/src/main/res/mipmap-${size}dpi
            convert icon_raw.png -resize ${size}x${size} app/src/main/res/mipmap-${size}dpi/ic_launcher.png
            cp app/src/main/res/mipmap-${size}dpi/ic_launcher.png app/src/main/res/mipmap-${size}dpi/ic_launcher_round.png
          done
          ls -la app/src/main/res/

      # ─── 6. BUAT PROJECT ANDROID NATIVE ──────────────────────────
      - name: 📦 Generate Android WebView Project
        run: |
          WEB_URL="${{ github.event.inputs.web_url }}"
          VERSION="1.0.0"
          APP_NAME="${{ github.event.inputs.app_name }}"
          PKG=$(echo "$APP_NAME" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]/_/g' | sed 's/__*/_/g' | sed 's/^_//' | sed 's/_$//')
          [ -z "$PKG" ] && PKG="webapp"
          
          flutter create --org com.web2apk --project-name "$PKG" webapp
          echo "PKG=$PKG" >> $GITHUB_ENV
          echo "APP_NAME=$APP_NAME" >> $GITHUB_ENV
          
          # Buat struktur project
          mkdir -p app/src/main/java/$(echo $PKG | tr '.' '/')
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/drawable
          
          # ─── AndroidManifest.xml ──────────────────────────────────
          cat > app/src/main/AndroidManifest.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android"
              package="PKG_PLACEHOLDER">
          
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
              <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
          
              <application
                  android:allowBackup="true"
                  android:icon="@mipmap/ic_launcher"
                  android:label="APP_NAME_PLACEHOLDER"
                  android:theme="@style/Theme.AppCompat.Light.NoActionBar"
                  android:usesCleartextTraffic="true">
                  
                  <activity
                      android:name=".MainActivity"
                      android:exported="true"
                      android:configChanges="orientation|screenSize|keyboardHidden">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF
          
          # Ganti placeholder
          sed -i "s/PKG_PLACEHOLDER/$PKG/g" app/src/main/AndroidManifest.xml
          sed -i "s/APP_NAME_PLACEHOLDER/$APP_NAME/g" app/src/main/AndroidManifest.xml
          
          # ─── MainActivity.java ────────────────────────────────────
          cat > app/src/main/java/$(echo $PKG | tr '.' '/')/MainActivity.java << 'EOF'
          package PKG_PLACEHOLDER;
          
          import android.os.Bundle;
          import android.webkit.WebView;
          import android.webkit.WebViewClient;
          import android.webkit.WebChromeClient;
          import android.webkit.WebSettings;
          import android.view.View;
          import android.view.WindowManager;
          import androidx.appcompat.app.AppCompatActivity;
          
          public class MainActivity extends AppCompatActivity {
              private WebView webView;
          
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  getWindow().setFlags(WindowManager.LayoutParams.FLAG_FULLSCREEN,
                          WindowManager.LayoutParams.FLAG_FULLSCREEN);
                  setContentView(R.layout.activity_main);
          
                  webView = findViewById(R.id.webView);
                  WebSettings webSettings = webView.getSettings();
                  webSettings.setJavaScriptEnabled(true);
                  webSettings.setDomStorageEnabled(true);
                  webSettings.setLoadWithOverviewMode(true);
                  webSettings.setUseWideViewPort(true);
                  webSettings.setBuiltInZoomControls(true);
                  webSettings.setDisplayZoomControls(false);
                  webSettings.setSupportZoom(true);
                  webSettings.setDefaultTextEncodingName("utf-8");
          
                  webView.setWebViewClient(new WebViewClient() {
                      @Override
                      public boolean shouldOverrideUrlLoading(WebView view, String url) {
                          view.loadUrl(url);
                          return true;
                      }
                  });
          
                  webView.setWebChromeClient(new WebChromeClient());
                  webView.loadUrl("WEB_URL_PLACEHOLDER");
              }
          
              @Override
              public void onBackPressed() {
                  if (webView.canGoBack()) {
                      webView.goBack();
                  } else {
                      super.onBackPressed();
                  }
              }
          }
          EOF
          
          # Ganti placeholder
          sed -i "s/PKG_PLACEHOLDER/$PKG/g" app/src/main/java/$(echo $PKG | tr '.' '/')/MainActivity.java
          sed -i "s|WEB_URL_PLACEHOLDER|$WEB_URL|g" app/src/main/java/$(echo $PKG | tr '.' '/')/MainActivity.java
          
          # ─── activity_main.xml ────────────────────────────────────
          cat > app/src/main/res/layout/activity_main.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical">
          
              <WebView
                  android:id="@+id/webView"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent" />
          
          </LinearLayout>
          EOF
          
          # ─── styles.xml ────────────────────────────────────────────
          cat > app/src/main/res/values/styles.xml << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <style name="Theme.AppCompat.Light.NoActionBar" parent="Theme.AppCompat.Light">
                  <item name="windowActionBar">false</item>
                  <item name="windowNoTitle">true</item>
                  <item name="android:windowFullscreen">true</item>
              </style>
          </resources>
          EOF
          
          # ─── build.gradle (app level) ─────────────────────────────
          cat > app/build.gradle << 'EOF'
          apply plugin: 'com.android.application'
          
          android {
              namespace 'PKG_PLACEHOLDER'
              compileSdk 34
          
              defaultConfig {
                  applicationId 'PKG_PLACEHOLDER'
                  minSdk 21
                  targetSdk 34
                  versionCode 1
                  versionName 'VERSION_PLACEHOLDER'
              }
          
              buildTypes {
                  release {
                      minifyEnabled true
                      proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
                  }
                  debug {
                      minifyEnabled false
                      debuggable true
                  }
              }
          
              compileOptions {
                  sourceCompatibility JavaVersion.VERSION_1_8
                  targetCompatibility JavaVersion.VERSION_1_8
              }
          }
          
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'androidx.core:core:1.12.0'
          }
          EOF
          
          sed -i "s/PKG_PLACEHOLDER/$PKG/g" app/build.gradle
          sed -i "s/VERSION_PLACEHOLDER/$VERSION/g" app/build.gradle
          
          # ─── proguard-rules.pro ────────────────────────────────────
          cat > app/proguard-rules.pro << 'EOF'
          -keep class android.webkit.** { *; }
          -keep class androidx.appcompat.** { *; }
          EOF
          
          # ─── build.gradle (root) ──────────────────────────────────
          cat > build.gradle << 'EOF'
          buildscript {
              repositories {
                  google()
                  mavenCentral()
              }
              dependencies {
                  classpath 'com.android.tools.build:gradle:8.2.0'
              }
          }
          
          allprojects {
              repositories {
                  google()
                  mavenCentral()
              }
          }
          
          task clean(type: Delete) {
              delete rootProject.buildDir
          }
          EOF
          
          # ─── settings.gradle ──────────────────────────────────────
          cat > settings.gradle << 'EOF'
          pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }
          dependencyResolutionManagement {
              repositories {
                  google()
                  mavenCentral()
              }
          }
          rootProject.name = 'WebViewApp'
          include ':app'
          EOF
          
          # ─── gradle.properties ─────────────────────────────────────
          cat > gradle.properties << 'EOF'
          org.gradle.jvmargs=-Xmx4g
          android.enableJetifier=true
          android.useAndroidX=true
          EOF
          
          echo "✅ Project Android WebView berhasil dibuat"

      # ─── 7. SETUP GRADLE WRAPPER ──────────────────────────────────
      - name: 🔧 Setup Gradle Wrapper
        run: |
          gradle wrapper --gradle-version 8.2
          chmod +x gradlew

      # ─── 8. BUILD APK ─────────────────────────────────────────────
      - name: 🔨 Build APK
        run: |
          echo "📦 Building APK..."
          ./gradlew assembleRelease --no-daemon --stacktrace
          echo "✅ Build selesai"

      # ─── 9. FIND APK ──────────────────────────────────────────────
      - name: 🔍 Find APK
        run: |
          APK_PATH=$(find app/build/outputs/apk/release -name "*.apk" | head -1)
          if [ -z "$APK_PATH" ]; then
            echo "❌ APK tidak ditemukan!"
            find app/build -name "*.apk" 2>/dev/null || echo "Tidak ada APK"
            exit 1
          fi
          echo "✅ APK ditemukan: $APK_PATH"
          echo "APK_PATH=$APK_PATH" >> $GITHUB_ENV
          ls -lh $APK_PATH

      # ─── 10. UPLOAD ARTIFACT ──────────────────────────────────────
      - name: 📤 Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: webview-apk-${{ github.event.inputs.app_name }}
          path: ${{ env.APK_PATH }}
          retention-days: 7
          compression-level: 0

      # ─── 11. UPLOAD KE RELEASE ────────────────────────────────────
      - name: 📤 Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: webview-${{ github.event.inputs.app_name }}-${{ github.run_number }}
          files: ${{ env.APK_PATH }}
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ─── 12. SUMMARY ──────────────────────────────────────────────
      - name: 📊 Build Summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ WEBVIEW APK BUILD SUCCESS!"
          echo "🌍 URL: ${{ github.event.inputs.web_url }}"
          echo "📱 App: ${{ github.event.inputs.app_name }}"
          echo "📁 APK: ${{ env.APK_PATH }}"
          APK_SIZE=$(du -h ${{ env.APK_PATH }} | cut -f1)
          echo "📏 Size: $APK_SIZE"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # ─── 13. NOTIFICATION ─────────────────────────────────────────
      - name: 🔔 Completion notification
        if: always()
        run: |
          echo "✅ Build selesai dengan status: ${{ job.status }}"
